// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  site: 'https://bakabo2st.github.io',
  base: '/partners_pfr',
  output: 'static',
});
